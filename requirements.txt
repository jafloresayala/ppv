# PPV Dashboard — dependencias para servidor
# Instalar con:
#   pip install -r requirements.txt

# ── Core web / API ─────────────────────────────────────────────────────────────
streamlit==1.51.0
requests==2.32.5
requests-ntlm==1.3.0          # Autenticación NTLM para SAP General API

# ── Data / numerics ────────────────────────────────────────────────────────────
pandas==2.3.3
numpy==1.26.4

# ── Visualización ──────────────────────────────────────────────────────────────
plotly==5.24.1

# ── Machine Learning / Estadística ────────────────────────────────────────────
scikit-learn==1.7.2            # IsolationForest, Ridge, GBR, KNN, scalers
scipy==1.16.3                  # stats, ANOVA
statsmodels==0.14.5            # SARIMAX, ARIMA, Holt-Winters, Theta, SES

# ── Forecasting avanzado ───────────────────────────────────────────────────────
prophet==1.3.0                 # Meta Prophet (modelo Bayesiano de series de tiempo)
neuralprophet==0.8.0           # NeuralProphet (Prophet + redes neuronales)
torch==2.10.0                  # PyTorch — requerido por NeuralProphet
holidays==0.95                 # Días festivos — dependencia de Prophet/NeuralProphet
tqdm==4.67.1                   # Barras de progreso — dependencia de NeuralProphet
matplotlib==3.10.6             # Graficación base — dependencia de Prophet

# ── Azure AI Foundry (chatbot tab) ────────────────────────────────────────────
azure-ai-inference==1.0.0b9
azure-core==1.39.0

# ── SSL (generación de certificado auto-firmado) ───────────────────────────────
cryptography==46.0.3
plotly==5.24.1